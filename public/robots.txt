# FitPlan AI - robots.txt
User-agent: *
Allow: /
Disallow: /api/
Disallow: /admin
# La landing /hyrox sí se indexa; /hyrox/plan es la app tras login y no aporta
# nada a un buscador (sirve un esqueleto vacío hasta que hay sesión).
Disallow: /hyrox/plan
# Paneles de usuario: no aportan nada en búsqueda y son cuenta ajena.
# Segunda barrera además del `noindex` de cada página — si un return
# temprano vuelve a dejar el meta fuera del HTML servido, esto lo cubre.
Disallow: /dashboard
Disallow: /_next/
Disallow: /404

# Sitemap
Sitemap: https://www.fitplan-ai.com/sitemap.xml

# Crawl-delay (optional, for polite bots)
User-agent: AhrefsBot
Crawl-delay: 10

User-agent: SemrushBot
Crawl-delay: 10
